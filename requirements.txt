# Pinned to known-good version — edgartools upstream has had silent behavioral
# changes between minor releases (EDGAR filing parser internals, XML schema handling).
# Do not loosen this pin without running the full test suite against the new version.
edgartools==5.31.5
openpyxl>=3.1
pandas>=2.0
numpy>=1.26
matplotlib>=3.8
pyyaml>=6.0
python-dateutil>=2.8
requests>=2.31
anthropic>=0.34  # optional, for LLM narrative
yfinance>=0.2.43  # for live market prices
python-telegram-bot>=20.3  # Phase 1: weekly digest + alerts (send-only)
